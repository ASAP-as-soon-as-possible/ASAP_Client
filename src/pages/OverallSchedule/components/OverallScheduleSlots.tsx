import { ColumnStructure } from 'components/timetableComponents/types';

function OverallScheduleSlots({ date, timeSlots }: ColumnStructure) {
  return <div>OverallScheduleSlots</div>;
}

export default OverallScheduleSlots;
